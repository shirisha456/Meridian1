alert_email = "you@example.com" # required — AWS Budgets notification target

# Optional. Leave blank to deploy against the Elastic IP with no domain.
# domain_name     = "meridian.example.com"
# route53_zone_id = "" # set only if the domain is already hosted elsewhere in Route 53
