# Read by docker-compose for optional root-level variable substitution
# (e.g. ${OPENAI_API_KEY:-}). Per-app/service .env files (apps/core-api/.env,
# etc.) hold the rest of the configuration and are added as each service
# is introduced in later phases.
OPENAI_API_KEY=
MARKET_DATA_API_KEY=
